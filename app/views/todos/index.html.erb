<%= link_to 'New Todo', {action: 'new'} %><br />
<ul>
  <% @todos.each do |todo| %>
  <li><h1><%= link_to todo.todo_title, {:action => 'view', :id => todo.id } %></h1></li>
    <p><%= todo.todo_text %></p>
  <% end %>
</ul>

<%= will_paginate @todos %>
